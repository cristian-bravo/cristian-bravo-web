---
const { title, description } = Astro.props;
---
<article class="card service-card">
  <h3>{title}</h3>
  <p>{description}</p>
  <style>
    .service-card h3 {
      margin: 0 0 12px;
    }

    .service-card p {
      color: var(--muted);
      margin: 0;
    }
  </style>
</article>
