---
import type { BlogPlaceholderContent } from '../../../data';

interface Props {
  content: BlogPlaceholderContent;
}

const { content } = Astro.props;
---
<section class="sr-only">
  <h1>{content.heading}</h1>
  <p>{content.description}</p>
</section>

