---
import ServiceModal from './ServiceModal.astro';
import type { ServicesModalContent } from '../../../data';

interface Props {
  modals: ServicesModalContent[];
}

const { modals } = Astro.props;
---
{modals.map((modal) => (
  <ServiceModal {...modal} />
))}

