export interface PageMetadata {
  title: string;
  description: string;
}

export interface SectionHeaderContent {
  kicker: string;
  title: string;
  description?: string;
}

export interface LinkActionContent {
  label: string;
  href: string;
}
